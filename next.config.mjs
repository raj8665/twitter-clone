/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["instagram.fjai1-3.fna.fbcdn.net", "lh3.googleusercontent.com"]
    }
};

export default nextConfig;
